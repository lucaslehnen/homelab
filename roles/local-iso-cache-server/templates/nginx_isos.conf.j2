server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /var/www/isos;

        server_name _;

        location / {
                autoindex on;
                try_files $uri $uri/ =404;
        }
}